--- /home/yunusemreyilmaz/omen-space/src/omen-gui/src/keyboardrgb.rs
+++ /home/yunusemreyilmaz/omen-space/src/omen-gui/src/keyboardrgb.rs
@@ -581,7 +581,19 @@
         state_json_opt = Some(state_json);
     }
 
-    let detected_mode = if specs.product_name.to_lowercase().contains("desktop") || specs.product_name.to_lowercase().contains("tower") {
+    let mut zone_override = 0;
+    if let Ok(home) = std::env::var("HOME") {
+        let path = format!("{}/.config/omenspace/settings.json", home);
+        if let Ok(json_str) = std::fs::read_to_string(&path) {
+            if let Ok(json) = serde_json::from_str::<serde_json::Value>(&json_str) {
+                if let Some(zo) = json.get("zone_override").and_then(|v| v.as_u64()) {
+                    zone_override = zo;
+                }
+            }
+        }
+    }
+
+    let detected_mode = if zone_override == 4 || (zone_override == 0 && (specs.product_name.to_lowercase().contains("desktop") || specs.product_name.to_lowercase().contains("tower"))) {
         KeyboardMode::DesktopRgb
-    } else if is_per_key {
+    } else if zone_override == 3 || (zone_override == 0 && is_per_key) {
         KeyboardMode::PerKey
-    } else if is_omen {
+    } else if zone_override == 1 || (zone_override == 0 && is_omen) {
         KeyboardMode::Omen4Zone
     } else {
         KeyboardMode::Victus1Zone
     };
 
@@ -848,7 +860,8 @@
 
-    if is_omen {
+    let is_omen_brand = is_omen || detected_mode == KeyboardMode::DesktopRgb;
+    if is_omen_brand {
         let lb_group = adw::PreferencesGroup::builder()
             .title(i18n::t("lightbar_group"))
             .description(i18n::t("lightbar_desc"))
